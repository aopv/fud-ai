{
  "name": "fud-ai-self-hosted",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "adapters/*"
  ],
  "scripts": {
    "build": "npm run build:server && npm run build --workspace @fud-ai/web-app",
    "build:server": "npm run build --workspace @fud-ai/sync-contracts && npm run build --workspace @fud-ai/server-core && npm run build --workspace @fud-ai/adapter-postgres && npm run build --workspace @fud-ai/server-node",
    "check": "npm run check --workspaces --if-present && npm run check:vercel",
    "check:vercel": "tsc -p tsconfig.vercel.json",
    "test": "npm run test --workspaces --if-present && npm run test:vercel",
    "test:vercel": "tsx --test deploy/vercel/*.test.ts",
    "dev": "npm run dev --workspace @fud-ai/web-app",
    "dev:server": "npm run dev --workspace @fud-ai/server-node"
  },
  "engines": {
    "node": ">=22"
  }
}
